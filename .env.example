# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Mistral AI Configuration
# Get this from https://console.mistral.ai/api-keys/
MISTRAL_API_KEY=your-mistral-api-key

# MCP Server Configuration (Optional - for enhanced development with Claude Desktop)
# Brave Search API - Get from https://api.search.brave.com/
BRAVE_SEARCH_API_KEY=your-brave-search-api-key

# PostgreSQL Direct Connection (for MCP PostgreSQL server)
# Get from Supabase Dashboard → Settings → Database → Connection string
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
POSTGRES_CONNECTION_STRING=your-postgres-connection-string

# SQLite Local Database Path (for MCP SQLite server)
# This will be created automatically if it doesn't exist
SQLITE_DB_PATH=./data/local.db

# Database Configuration (for development)
# Set to 'sqlite' for local development, 'supabase' for production
DATABASE_TYPE=supabase